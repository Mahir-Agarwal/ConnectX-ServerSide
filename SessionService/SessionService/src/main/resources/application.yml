spring:
  application:
    name: session-service
  redis:
    host: ${REDIS_HOST:localhost}
    port: ${REDIS_PORT:6379}
    password: ${REDIS_PASSWORD:}

server:
  port: 8081
  address: 0.0.0.0

eureka:
  client:
    service-url:
      defaultZone: "${EUREKA_URI:http://localhost:8761/eureka}"
    register-with-eureka: true
    fetch-registry: true
  instance:
    hostname: session-service.railway.internal
    prefer-ip-address: false

jwt:
  secret: "${JWT_SECRET:woidw0-9waj-9dawidnaiwndianidanipdnandianipdnapawdaopwndpawnd}"
